server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore?useSSL=true&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: wisdomlight9
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.bookstore.entity

logging:
  level:
    com.bookstore.mapper: debug
